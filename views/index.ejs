<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css"
      integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh"
      crossorigin="anonymous"
    />
    <title>Url Shortener</title>
  </head>
  <body>
    <div class="container">
      <h1 class="bg-white bg-gradient text-black py-3 mt-4">
        Welcome to URL Shortener Service
      </h1>
      <form action="/shortUrls" method="POST" class="my-4 form-inline">
        <label for="fullUrl" class="sr-only">Url</label>
        <input
          required
          placeholder="Url"
          type="url"
          name="fullUrl"
          id="fullUrl"
          class="form-control col mr-2"
        />
        <input
          placeholder="alias"
          type="string"
          name="alias"
          id="alias"
          class="form-control mr-2"
        />
        <button type="submit" id="currentShortenedUrl" class="btn btn-success">
          Submit
        </button>
      </form>

      <div id="shortenedUrl" style="display: none">
        <p><b>Your Shortened URL :&nbsp;</b></p>
        <a
          href="<%= shortUrls && Object.keys(shortUrls).length > 0 ? shortUrls[Object.keys(shortUrls)[Object.keys(shortUrls).length - 1]].short : '' %>"
        >
          <%= shortUrls && Object.keys(shortUrls).length > 0 &&
          shortUrls[Object.keys(shortUrls)[Object.keys(shortUrls).length - 1]] ?
          `https://urlshortener-dev.onrender.com/${shortUrls[Object.keys(shortUrls)[Object.keys(shortUrls).length
          - 1]].short}` : '' %>
        </a>
      </div>

      <button id="showUrlsBtn" class="btn btn-dark">Show My URL's</button>

      <div id="urlsTableDiv" style="display: none">
        <table class="table table-striped table-responsive text-black mt-4">
          <thead>
            <tr>
              <th>Full URL</th>
              <th>Short URL</th>
            </tr>
          </thead>
          <tbody>
            <% shortUrls.forEach(shortUrl => { %>
            <tr>
              <td>
                <a href="<%= shortUrl.full %>"><%= shortUrl.full %></a>
              </td>
              <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
    <script>
      const showUrlsBtn = document.getElementById("showUrlsBtn");
      const urlsTableDiv = document.getElementById("urlsTableDiv");
      const specificElement = document.getElementById("specificElement");
      const shortenedUrl = document.getElementById("shortenedUrl");
      const form = document.querySelector("form");

      showUrlsBtn.addEventListener("click", function () {
        urlsTableDiv.style.display = "block";
      });

      form.addEventListener("submit", function (event) {
        const fullUrlInput = document.getElementById("fullUrl");
        if (fullUrlInput.value.trim() === "") {
          event.preventDefault(); // Prevent form submission
          fullUrlInput.focus(); // Set focus to the empty input field
        } else {
          shortenedUrl.style.display = "flex";
          event.preventDefault();
        }
      });

    </script>
  </body>
</html>
